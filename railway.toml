# railway.toml
[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "python init_db.py"
startCommand = "python app.py" # CRITICAL: Initialize DB FIRST
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
